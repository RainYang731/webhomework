# 月兔防衛隊

## 目標

製作一款以中秋節為主題、可在 GitHub Pages 公開遊玩的手機網頁遊戲，玩法是快速點擊並消除畫面中的殭屍圖片。

## 主要玩法

- 遊戲畫面優先填滿手機直向螢幕；中央遊戲區固定為 4 欄 × 7 列，白底黑色格線。
- 開始前就顯示 4×7 格棋盤；每個橫排恰好隨機出現一隻殭屍，其他格保持空白。開始遊戲後，每局限時 60 秒。
- 點擊一張圖片可將它消除並得 1 分。
- 同一欄任何格子都可作為點擊區，命中判定一律選該欄最下方的殭屍；只有整欄沒有殭屍才算點錯。
- 消除後，該排上方的殭屍往下落一排；最上排再從 4 欄隨機選一格生成新圖片，讓每個橫排繼續只有一隻殭屍。
- 點到沒有殭屍的欄位算失誤，接下來 1 秒不能操作；失誤時最底排的殭屍會閃爍提示。
- 點擊判定只看橫向欄位，忽略縱向位置；同一欄內按哪一排都命中最下面的殭屍。
- 命中後的操作鎖定與圖片落下動畫約 50 毫秒，讓玩家可快速連續操作。
- 圖片由專案提供，玩家端不提供上傳介面。遊戲使用 `assets/zombie.svg`；可在 GitHub 編輯或替換此素材，統一改變所有格子的圖片。
- 時間結束後顯示本局成績並更新本機排行榜。

## 畫面與操作

- 畫面顯示倒數時間、目前分數、玩家名稱、排行榜按鈕與開始／再玩按鈕。
- 手機直向時遊戲畫面貼合可用視窗高度，不需捲動頁面；格子會隨螢幕尺寸伸縮。
- 支援觸控與滑鼠點擊。

## 排行榜與資料保存

- 純 GitHub Pages 前端不能安全地直接寫入 Git 儲存庫，因此使用瀏覽器 `localStorage` 保存本機排行榜。
- 排行榜依分數由高至低排列，保留前 10 名；同分時較早達成者優先。
- 排行榜只會在同一瀏覽器、同一裝置可見，不是跨玩家共用的線上排行榜。

## Acceptance Criteria

- 開始前及遊戲過程中，每個橫排都恰好只有一隻殭屍，其橫向位置隨機選自 4 格。
- 每次點擊消除一張圖片只增加 1 分。
- 點中殭屍後，上方殭屍快速下落一排，頂端在 4 格中隨機補入一隻新殭屍。
- 點擊判定只依左至右的欄位座標，不受上至下的點擊位置影響；同欄任何格都命中該欄最下方的殭屍。
- 點到沒有殭屍的欄位不加分，鎖定操作 1 秒，並讓最底排殭屍閃爍；命中後操作鎖定約 50 毫秒。
- 一局倒數 60 秒，結束後停止計分並顯示成績。
- 成績會記錄在本機排行榜，重整頁面後仍存在。
- 玩家不能上傳圖片；開發者可在 GitHub 編輯 `assets/zombie.svg` 統一替換圖片。
- 手機直向遊戲畫面填滿可用視窗，不需捲動。

## Non-goals

- 不提供跨裝置或跨玩家共用的線上排行榜。
- 不實作登入、後端 API、資料庫或玩家圖片上傳。
